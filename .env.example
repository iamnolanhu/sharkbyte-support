# ===========================================
# SharkByte Support - Environment Variables
# ===========================================
#
# EASY DEPLOYMENT: Only DO_API_TOKEN is required!
# All other variables are auto-discovered or created.
#
# IMPORTANT: See DEPLOYMENT.md for the complete setup guide, including:
# - Prerequisites (one-time manual agent creation required)
# - Estimated costs (~$20/month for database + AI usage)
# - Post-deployment optimization
# - Troubleshooting common issues
#
# The app will:
# 1. Create a "SharkByte Support" project in DO if needed
# 2. Provision a vector database automatically (~$19.60/month)
# 3. Create a demo agent for your deployment domain
# 4. The demo widget works without NEXT_PUBLIC_* vars (runtime fallback)
#
# After first deployment, check the build logs for recommended
# env vars to add (especially DO_DATABASE_ID to prevent duplicate DBs!).
#

# ===========================================
# REQUIRED - DigitalOcean API Token
# ===========================================
# Get from: https://cloud.digitalocean.com/account/api/tokens
# Scopes needed: read, write
DO_API_TOKEN=your_digitalocean_api_token

# ===========================================
# OPTIONAL - App Domain Configuration
# ===========================================
# Set this to your custom domain
# Falls back to: VERCEL_PROJECT_PRODUCTION_URL → sharkbyte-support.vercel.app
APP_DOMAIN=

# ===========================================
# OPTIONAL - Speed Optimizations
# ===========================================
# These are auto-discovered, but setting them skips API calls:

# DigitalOcean Project ID (auto-created if not set)
# Get from build logs after first deployment
DO_PROJECT_ID=

# Shared Database ID for Knowledge Bases (auto-provisioned if not set)
# Get from build logs after first deployment
DO_DATABASE_ID=

# Model Access Key ID (prevents duplicate key creation)
# Get from build logs after first deployment, or from:
# https://cloud.digitalocean.com/gen-ai/model-access-keys
DO_MODEL_ACCESS_KEY_ID=

# NOTE: DO_WORKSPACE_ID is NOT supported - the DO API does not allow
# workspace assignment during agent creation. Agents are created in the
# default workspace and must be moved manually via Control Panel.
# See DEPLOYMENT.md troubleshooting section for details.

# Demo widget config (uses runtime fallback if not set)
# Get from build logs after first deployment
NEXT_PUBLIC_DEMO_AGENT_ENDPOINT=
NEXT_PUBLIC_DEMO_AGENT_ACCESS_KEY=

# ===========================================
# OPTIONAL - Model Configuration
# ===========================================
# Only change these if you want different models

# Region (tor1 is the only region supporting gen-ai agents)
DO_REGION=tor1

# Embedding Model UUID
# Default: gte-large-en-v1.5 (22653204-79ed-11ef-bf8f-4e013e2ddde4)
# Options: mpnet (22652f75...), minilm (22652c2a...)
DO_EMBEDDING_MODEL_UUID=

# LLM Model UUID
# Default: GPT-oss-120b (18bc9b8f-73c5-11f0-b074-4e013e2ddde4)
# Options: gpt-4o-mini (9a3644c7...), llama3.3-70b (d754f2d7...)
DO_LLM_MODEL_UUID=

# ===========================================
# OPTIONAL - Firecrawl Fallback
# ===========================================
# For sites with JavaScript rendering issues
# Get from: https://firecrawl.dev
FIRECRAWL_API_KEY=
